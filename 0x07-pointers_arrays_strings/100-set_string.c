#include "main.h"

/**
 * set_string - set a string to another string
 *@s: a pointer to a string to set
 *@to: a ppointer to a string which we set to
 *
 * Return: nothing
 */

void set_string(char **s, char *to)
{
	*s = to;
}
