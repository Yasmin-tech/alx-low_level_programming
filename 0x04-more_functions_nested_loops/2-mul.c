#include "main.h"

/**
 * mul - a function that multiples two digits
 * @a: the first number
 * @b: the second number
 * Return: the result of a * b
 */

int mul(int a, int b)
{
	int mul_result = a * b;

	return (mul_result);
}
